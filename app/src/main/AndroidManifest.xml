<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dinihvebrahim.tabu3x">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".benKimim"
            android:screenOrientation="landscape"/>
        <activity android:name=".susarakAnlat" />
        <activity android:name=".takimlar" />
        <activity android:name=".oyunlar" />
        <activity android:name=".oyuna_basla" />
        <activity android:name=".magaza" />
        <activity android:name=".hakkinda" />
        <activity android:name=".ayarlar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>